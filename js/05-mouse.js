/*
 * Події миші
 * - mouseenter і mouseleave (це ховер)
 * - mouseover і mouseout
 * - mousemove (chatty event - балакуча подія)
 */
// const boxElem = document.querySelector('.js-box');

/* 
boxElem.addEventListener('mouseenter', onMouseEnter);
boxElem.addEventListener('mouseleave', onMouseLeave);

function onMouseEnter() {
  boxElem.classList.add('box--active');
}
function onMouseLeave() {
  boxElem.classList.remove('box--active');
} */

/* boxElem.addEventListener('mouseover', onMouseEnter);
boxElem.addEventListener('mouseout', onMouseLeave);

function onMouseEnter() {
  console.log('sefsefsef');
  boxElem.classList.add('box--active');
}
function onMouseLeave() {
  console.log('awdawd');
  boxElem.classList.remove('box--active');
} */

//============

// const boxElem = document.querySelector('.js-box');
// const child = boxElem.firstElementChild;
// child.style.position = 'absolute';
// boxElem.addEventListener('mousemove', onMouseMove);

// function onMouseMove(e) {
//   child.style.left = `${e.x - 50}px`;
//   child.style.top = `${e.y - 50}px`;
// }
